spring:
  datasource:
    url: ${PM_AGENT_DATABASE_URL:jdbc:mysql://127.0.0.1:3306/pm_agent?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai}
    username: ${PM_AGENT_DATABASE_USERNAME:root}
    password: ${PM_AGENT_DATABASE_PASSWORD:@Admin123456}

pm-agent:
  llm:
    primary:
      base-url: https://coding.dashscope.aliyuncs.com/v1
      model: qwen3.6-plus
      api-key: sk-sp-b75d8ad0933449eb89d6cf52706c716e
      timeout-seconds: 60
      retry-times: 1
    fallback:
      base-url: ${PM_AGENT_LLM_FALLBACK_BASE_URL:https://api.openai.com/v1}
      model: ${PM_AGENT_LLM_FALLBACK_MODEL:gpt-4.1-mini}
      api-key: ${PM_AGENT_LLM_FALLBACK_API_KEY:}
      timeout-seconds: ${PM_AGENT_LLM_FALLBACK_TIMEOUT_SECONDS:30}
      retry-times: ${PM_AGENT_LLM_FALLBACK_RETRY_TIMES:1}
