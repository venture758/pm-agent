## 1. 模型与存储

- [x] 1.1 新增知识更新业务模块前后记录模型，定义 `requirement_id`、`before_snapshot`、`after_snapshot`、`diff_summary`、`changed` 等字段
- [x] 1.2 为模块级知识更新前后记录新增数据库表与仓储读写能力，支持按 `workspace_id + session_id + requirement_id` 查询
- [x] 1.3 扩展知识更新会话级记录摘要，补充受影响模块数和模块级记录可用性标记

## 2. 知识更新链路接线

- [x] 2.1 在知识更新 Agent 自动更新业务模块前按需求采集受影响模块的 `before_snapshot`
- [x] 2.2 在自动更新完成后生成 `after_snapshot` 和字段级 `diff_summary`，并落库模块级记录
- [x] 2.3 明确成功、无变化、跳过、失败场景的记录语义，避免伪造 `after_snapshot`

## 3. API 与页面展示

- [x] 3.1 扩展确认提交返回 payload，返回最近一次知识更新的模块变更摘要
- [x] 3.2 扩展确认历史查询结果和详情查询能力，返回 `workspace_id + session_id + requirement_id` 维度的模块前后记录或其结构化摘要
- [x] 3.3 更新确认中心页面，在最近一次知识更新和确认历史中展示业务模块更新前后记录

## 4. 测试与回归

- [x] 4.1 补充后端测试，覆盖成功、有命中但无变化、跳过、失败四类知识更新记录场景
- [x] 4.2 补充前端测试，覆盖确认中心和确认历史对模块前后记录的展示
- [x] 4.3 执行回归测试，验证知识更新记录、确认历史和确认中心展示链路可用
