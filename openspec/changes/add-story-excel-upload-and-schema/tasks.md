## 1. 字段基线与数据模型

- [x] 1.1 从 `docs/导出列表_用户故事列表_0410191819.xlsx` 提取并固化故事模板表头清单（45 列）
- [x] 1.2 设计并实现 `EXCEL_HEADER -> db_column` 映射（包含必要别名与规范化规则）
- [x] 1.3 新增 `workspace_story_records` 的 MySQL DDL（字段列 + 唯一键 `workspace_id,user_story_code`）
- [x] 1.4 在 SQLite schema 初始化中新增 `workspace_story_records` 表定义

## 2. 后端导入能力

- [x] 2.1 新增故事 Excel 解析器，支持表头校验、空行过滤与类型转换
- [x] 2.2 实现故事记录 upsert（按 `workspace_id + user_story_code` 幂等）
- [x] 2.3 新增 story-only 上传服务方法，返回 `total/created/updated/failed` 与错误明细
- [x] 2.4 新增 API 路由（仅接收 `story_file`）并接入统一错误处理

## 3. 前端故事管理页改造

- [x] 3.1 在故事管理页面新增“单独上传故事 Excel”入口与按钮状态控制
- [x] 3.2 新增前端 API client/store 方法调用 story-only 上传接口
- [x] 3.3 展示导入结果摘要与失败明细（包括缺失列、行级错误）
- [x] 3.4 导入成功后刷新故事列表数据源并保持现有筛选/排序可用

## 4. 兼容与迁移

- [x] 4.1 保持现有“故事+任务双文件同步”接口行为不变
- [x] 4.2 定义新旧故事数据读取优先级（新表优先，旧快照兜底）
- [x] 4.3 编写迁移说明与回滚步骤（接口回滚、表结构保留）

## 5. 测试与验收

- [x] 5.1 增加故事表头校验与字段映射单元测试（含关键列缺失场景）
- [x] 5.2 增加 story-only 上传 API 集成测试（成功、部分失败、全失败）
- [x] 5.3 增加前端 store/API 流程测试（上传状态、结果展示、错误提示）
- [x] 5.4 执行回归测试，确认监控/洞察/任务管理流程无回归
